development:
    adapter: mysql2
    encoding: utf8
    reconnect: true
    database: timecontrol_development
    pool: 5
    username: root
    password:
    timeout: 5000         

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: mysql2
    encoding: utf8
    reconnect: true
    database: timecontrol_test
    pool: 5
    username: root
    password:
    timeout: 5000

production:
    adapter: mysql2
    encoding: utf8
    reconnect: true
    database: timecontrol
    pool: 5
    username: root
    password:
    timeout: 5000
